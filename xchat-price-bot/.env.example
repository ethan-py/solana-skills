# Copy to .env (gitignored) and fill in. Never commit real values.

# App-only bearer token from the X developer portal (only needed to register the bot)
X_APP_BEARER_TOKEN=

# Minted by register_bot.py — shown once, save it here immediately
XCHAT_BOT_TOKEN=
XCHAT_BOT_ID=

# Optional tuning
# XCHAT_POLL_INTERVAL=5
# XCHAT_ALERT_INTERVAL=30
